language: ruby
install: sudo apt-get install graphviz -y
before_install:
 - gem install bundler
 - bundle install
script: bundle exec rake
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.1
  - jruby-19mode
  - rbx-2